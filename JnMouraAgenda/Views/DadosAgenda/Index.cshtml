@model IEnumerable<JnMouraAgenda.Models.DadosAgenda>

@{
    ViewBag.Title = "Lista Agenda";
}

<h2>Lista de Registros</h2>

<p>
    @Html.ActionLink("Criar Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Telefone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Endereco)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Telefone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Endereco)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdTelefone }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.IdTelefone }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.IdTelefone })
        </td>
    </tr>
}

</table>
